{
  "buildCommand": "prisma generate && next build && next-sitemap",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "env": {
    "NEXT_PUBLIC_NODE_ENV": "production"
  },
  "crons": [
    {
      "path": "/api/cron/generate-daily-posts",
      "schedule": "0 9 * * *"
    }
  ]
}